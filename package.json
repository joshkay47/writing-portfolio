{
  "name": "writing-portfolio",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "sync": ".venv/bin/python scripts/sync_data.py",
    "setup:sync": "python3 -m venv .venv && .venv/bin/pip install -q -r scripts/requirements.txt"
  },
  "dependencies": {
    "astro": "^7.3.5",
    "d3": "^7.9.0",
    "topojson-client": "^3.1.0"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
